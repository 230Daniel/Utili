// Create "config.js" with this contents, then modify the settings.

window.__config = {
	backend: "https://localhost:5001",	// Your publicly accessible backend url - when testing locally this will be https://localhost:5001 by default
	clientId: ""						// Your Discord bot's client ID
};
