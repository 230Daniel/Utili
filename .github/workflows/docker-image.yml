name: Docker Image CI

on:
  push:
    branches: [ "main" ]

jobs:

  build-bot:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Build the Utili Bot Docker image
      run: docker build ./src/Utili.Bot --file ./Dockerfile --tag utili-bot:$(date +%s)

  build-backend:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Build the Utili Backend Docker image
      run: docker build ./src/Utili.Backend --file ./Dockerfile --tag utili-backend:$(date +%s)
  
  build-frontend:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Build the Utili Frontend Docker image
      run: docker build ./src/Frontend --file ./Dockerfile --tag utili-frontend:$(date +%s)
